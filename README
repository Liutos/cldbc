This package contains the functions and macros for interacting with MySQL database in Common Lisp. In this version, the user can only operate on one database because it doesn't provide the functionality for changing yet. Now this package privides the functionalities including select, insert, update and delete. It's highly recommended that the user should use the package-defined class result for storing the query result each time and process the result by means of the function for this class.